how to install:

#setup the machine

sudo apt-get python-dev gcc
sudo easy_install pip supervisor
pip install virtualenvwrapper
mkvirtualenv asteriskdb
workon asteriskdb
pip install -r requirements.txt

python manage.py syncdb #enter the admin information to login into admin-site

